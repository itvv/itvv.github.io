var timeout=null;window.onresize=function(){clearTimeout(timeout);timeout=setTimeout(function(){window.location.reload()},100);};function adjust(){var map=document.getElementById("news");var element=map.childNodes;var itemNumber=element.length/2;for(var i=0;i<itemNumber-1;i+=1){var item=2*i+1;var oldCoords=element[item].coords;var newcoords=adjustPosition(oldCoords);element[item].setAttribute("coords",newcoords)}}function adjustPosition(position){var pageWidth=document.body.clientWidth;var pageHeith=document.body.clientHeight;var imageWidth=1154;var imageHeigth=1664;var each=position.split(",");for(var i=0;i<each.length;i+=1){each[i]=Math.round(parseInt(each[i])*pageWidth/imageWidth).toString();i+=1;each[i]=Math.round(parseInt(each[i])*pageHeith/imageHeigth).toString();}var newPosition="";for(var i=0;i<each.length;i+=1){newPosition+=each[i];if(i<each.length-1){newPosition+=","}}return newPosition}